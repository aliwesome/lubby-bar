{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Permissions for the Lubby Bar panel window.",
    "windows": ["panel"],
    "permissions": ["core:default", "opener:default"]
}
